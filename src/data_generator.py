import os\nfrom faker import Faker\n\nclass DataGenerator:\n    def __init__(self):\n        self.faker = Faker()\n\n    def generate_data(self, num_records):\n        data = []\n        for _ in range(num_records):\n            record = {\n                'name': self.faker.name(),\n                'address': self.faker.address(),\n                'email': self.faker.email(),\n                'birthdate': self.faker.date_of_birth().isoformat()\n            }\n            data.append(record)\n        return data\n\nif __name__ == '__main__':\n    generator = DataGenerator()\n    num_records = 10  # Adjust the number of records you want to generate\n    simulated_data = generator.generate_data(num_records)\n    print(simulated_data)\n